# SOLHackathon

This repository showcases a minimal Solana payment interface served by a small Node.js server.

## Getting Started

1. Install dependencies (none required by default).
2. Start the development server:
   ```bash
   npm start
   ```
3. Open your browser at `http://localhost:3000`.

## Testing

Run the test suite with:
```bash
npm test
```
